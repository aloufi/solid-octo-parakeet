import { EmployeeObject } from '../vacation/model/employee-object';

describe('EmployeeObject', () => {
  it('should create an instance', () => {
    expect(new EmployeeObject()).toBeTruthy();
  });
});
